package com.anotether.ui.theme

import androidx.compose.ui.graphics.Color

// ── Brand palette ─────────────────────────────────────────────────────────────
// Dark navy base, burnt orange accents, white text.
// Designed to feel serious, private, and trustworthy — not playful.

val Navy950 = Color(0xFF0A0E1A)   // Deepest background
val Navy900 = Color(0xFF0F1525)   // Primary background
val Navy800 = Color(0xFF161E33)   // Card / surface
val Navy700 = Color(0xFF1E2A42)   // Elevated surface
val Navy600 = Color(0xFF2A3A58)   // Border / divider
val Navy500 = Color(0xFF3B4F72)   // Subtle accent bg

val BurntOrange = Color(0xFFCC5500)     // Primary action
val BurntOrangeLight = Color(0xFFE06620) // Hover / highlight
val BurntOrangeDim = Color(0xFF8A3A00)  // Disabled / muted

val White = Color(0xFFFFFFFF)
val OffWhite = Color(0xFFE8EAF0)
val SubtleGray = Color(0xFF8892A8)
val DimGray = Color(0xFF4A5568)

val ErrorRed = Color(0xFFE53E3E)
val SuccessGreen = Color(0xFF38A169)

// Message bubble colors
val OutgoingBubble = Color(0xFFCC5500)    // Burnt orange — our messages
val IncomingBubble = Color(0xFF1E2A42)   // Navy — their messages
val OutgoingText = Color(0xFFFFFFFF)
val IncomingText = Color(0xFFE8EAF0)
